'use strict';

import React from 'react';
import {
  StyleSheet
} from 'react-native';

const GroupStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
  },
});

export default GroupStyles;
